---
title: Works
slug: "/works"
---

import { projectCatalogue } from "../../../src/apps/catalog";

This build-time index contains reviewed deployed work from owned public repositories. Projects without a valid credential-free HTTPS homepage are intentionally omitted. In Tien OS, retained projects open in a responsive embedded window; the links in this document view open the deployment in a protected new tab.

{projectCatalogue.map((project) => (

  <section>
    <h2>{project.name}</h2>
    <p>{project.summary}</p>
    <p>
      <a href={project.route}>View project details</a>
      {" · "}
      <a href={project.target.url} target="_blank" rel="noopener noreferrer">
        Launch project
        <span className="sr-only"> in a new tab</span>
      </a>
    </p>
  </section>
))}
