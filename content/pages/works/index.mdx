---
title: Works
slug: "/works"
---

import { projectCatalogue } from "../../../src/apps/catalog";

This is the complete build-time index of owned public GitHub repositories and selected deployed work. Each project opens outside Tien OS at its credential-free HTTPS homepage when available, or at its exact GitHub repository URL.

{projectCatalogue.map((project) => (

  <section>
    <h2>{project.name}</h2>
    <p>{project.summary}</p>
    <p>
      <a href={project.route}>View project details</a>
      {" · "}
      <a href={project.target.url} target="_blank" rel="noopener noreferrer">
        Launch project
        <span className="sr-only"> in a new tab</span>
      </a>
    </p>
  </section>
))}
